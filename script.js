let posts=[];const esc=s=>String(s??"").replace(/[&<>"']/g,m=>({"&":"&amp;","<":"&lt;",">":"&gt;",'"':"&quot;","'":"&#039;"}[m]));async function load(){try{let r=await fetch("data/posts.json");posts=(await r.json()).posts||[]}catch(e){posts=[]}render(posts)}function render(list){document.querySelector("#latest").innerHTML=list.map((p,i)=>`<article class="card" onclick="location.href='posts/${p.slug}.html'"><div class="cover" style="background:linear-gradient(145deg,#${["71323e","174e82","68452a","4b2a70"][i%4]},#071321)">${p.images?.[0]?`<img src="${p.images[0]}" alt="${esc(p.title)} - Page 1">`:esc(p.manga||p.title)}</div><div class="body"><h3>${esc(p.title)}</h3><div class="meta">${esc(p.chapter||"")} · ◉ ${esc(p.views||"0")}</div><div class="meta rating">★ ${esc(p.rating||"")}</div></div></article>`).join("")||"<p style='padding:20px;color:#91a5bd'>まだ投稿がありません。</p>";document.querySelector("#rank").innerHTML=list.slice(0,5).map((p,i)=>`<div class="rank"><div class="num">${i+1}</div><div class="rcover">${esc(p.manga||p.title)}</div><div><div class="rname">${esc(p.manga||p.title)}</div><div class="rmeta">★ ${esc(p.rating||"")} · ◉ ${esc(p.views||"")}</div></div></div>`).join("")}document.querySelector("#q").oninput=e=>{let q=e.target.value.toLowerCase();render(posts.filter(p=>(p.title+" "+p.manga+" "+p.chapter+" "+p.genre).toLowerCase().includes(q)))};document.querySelectorAll(".tabs button").forEach(b=>b.onclick=()=>{document.querySelectorAll(".tabs button").forEach(x=>x.classList.remove("on"));b.classList.add("on");let g=b.dataset.g;render(g==="all"?posts:posts.filter(p=>p.genre===g))});load();